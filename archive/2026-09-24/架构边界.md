# 蓝色大肥鱼 · 架构边界

> 文档状态：规范基准
>
> 建立日期：2026-09-22
>
> 与 [`站点升级路线图.md`](站点升级路线图.md) 的分工：本文管「现在边界在哪、改东西动哪里」，路线图管远期方向；两者不一致时，当前约束以本文为准。

---

## 0. 这份文档是什么

给改这个仓库的人和代理读的**边界基准**：一块东西归谁、改它动哪几处、什么不许破。它不复述任何一份文档的正文，只画边界、给索引、定规则。为接下来的前端风格大改、布局重排、分类体系重建打底——那三件事动的都是「边界内的实现」，不动边界本身。

与其它文档的关系：

| 文档 | 管什么 | 与本文的分工 |
|---|---|---|
| [`AGENTS.md`](AGENTS.md) | 动手须知、已知问题、本机环境坑 | 那是操作手册，这是规则来源；硬约定在 §7 汇总 |
| [`数据契约.md`](数据契约.md) | 字段与枚举的唯一来源 | 本文只引用，不抄字段定义 |
| [`CONTRIBUTING.md`](CONTRIBUTING.md) | 投稿与开发流程、视觉规矩细则、评论区实现 | 流程归它，边界归本文 |
| [`docs/维护与发布.md`](docs/维护与发布.md) | 发布拓扑、`ops/` 脚本用法 | §5 只画边界，细节留在那里 |
| [`站点升级路线图.md`](站点升级路线图.md) | 远期方向（1A→1D、后端、统计评估） | 本文管现状，它管未来 |
| [`CHANGELOG.md`](CHANGELOG.md) | 变更历史与「大更新先记日志」规则 | §7 把那条规则列为硬规则 |

## 1. 分层地图

六层，层与层之间只有标注的通道是合法的；绕过通道直接写对面的层，就是在制造下一次「清单改了、前端找不到文件」。

```text
┌─ 内容源 ─────────────────────────────────────────────────────┐
│ 投稿者（GitHub Issue 表单） · 上游 EDMOK/blue-fish-archive     │
│ 站长自产 · 仓库外导入流程（首批清单）                           │
└───────────────────────────────────────────────────────────────┘
        │ 写数据只有两条路：GitHub Issue 投稿落库、维护者 git 提交
        ▼
┌─ 数据清单 ────────────────────────────────────────────────────┐
│ dist/characters.json · dist/categories.json                    │
│ dist/submissions/（works.json + previews/ + large/ + originals/）│
│ dist/owner-picks/（works.json + previews/）· owner-picks/（原图）│
│ dist/blue-fish-ids.json（首批 ID / slug 冻结映射，进 git）       │
│ dist/data/blue-fish-classification.json（不进 git）             │
└───────────────────────────────────────────────────────────────┘
        │ 两条通道：页面 fetchJson（no-cache）加载清单；
        │ tools/prepare_works.mjs + tools/generate_work_pages.mjs 读清单产出详情页
        ▼
┌─ 前端运行时 ──────────────────────────────────────────────────┐
│ dist/ 多页静态站：index.html + 分类 / 投稿 / 关于 / 推荐 /       │
│ 更新日志页 + works/<slug>.html（详情页生成物，191 个）           │
│ styles.css + tokens.css（旧单页 app.js 已退役删除）             │
└───────────────────────────────────────────────────────────────┘
        │ 唯一通道：tools/build.mjs（零依赖、确定性复制 + 断言）
        ▼
┌─ 构建产物 ────────────────────────────────────────────────────┐
│ 一份干净静态目录（不含 dist/data/ 与 submissions/originals/）    │
└───────────────────────────────────────────────────────────────┘
        │ 唯一通道：ops/deploy-server.sh（服务器侧执行，GitHub 驱动）
        ▼
┌─ 发布拓扑 ────────────────────────────────────────────────────┐
│ $DEPLOY_ROOT：source/ → releases/<时间戳> → current 软链        │
└───────────────────────────────────────────────────────────────┘
        │ 浏览器只读；原图下载跳 GitHub Raw；写回仍只有上面那两条路
        ▼
┌─ 外部服务 ────────────────────────────────────────────────────┐
│ GitHub（Issue 写入口 · Raw 原图托管） · Giscus（评论）           │
│ 未来的判断服务（只许存在于服务器侧自动审核 bot 层）               │
└───────────────────────────────────────────────────────────────┘
```

要点：数据往下流是单向的——清单被前端读、前端被打包、产物被发布；**没有任何一层向上写**。访客能产生的写入只有一种形态：GitHub Issue。评论是外部服务自己的数据，不回流（[`数据契约.md`](数据契约.md) §12）。

## 2. 数据边界

### 2.1 清单与图片的归属

| 清单 / 目录 | 谁写 | 进 git | 形状定义在哪 |
|---|---|---:|---|
| `dist/characters.json`（角色，11 条含 owner-picks） | 维护者 git 提交 | 是 | [`数据契约.md`](数据契约.md) §4 |
| `dist/categories.json`（分类轴 v0 草案，8 条） | 维护者 git 提交 | 是 | `数据契约.md`「分类记录」章节 |
| `dist/submissions/works.json` | 投稿落库 | 是 | `数据契约.md` §2、§3 |
| `dist/submissions/previews/`、`dist/submissions/large/` | `tools/generate_image_derivatives.py` | 是 | `数据契约.md` §3 的 `thumbnailPath` / `fullPath` |
| `dist/submissions/originals/`（投稿原图） | 投稿落库 | 是（**不进发布产物**，线上走 GitHub Raw） | — |
| `dist/owner-picks/works.json` + `dist/owner-picks/previews/` | 维护者 git 提交 | 是 | `数据契约.md` §2、§3 |
| `dist/blue-fish-ids.json`（首批 ID / slug 冻结映射） | `tools/prepare_works.mjs`（幂等，只补缺） | 是 | `数据契约.md` §2.1 |
| `dist/works/`（191 个 `works/<slug>.html` 详情页） | `tools/generate_work_pages.mjs`（幂等全量重建） | 是 | [`docs/SEO规范.md`](docs/SEO规范.md) |
| `owner-picks/`（仓库根，4 张站长自用原图） | 维护者 git 提交 | 是（`path` 指向它的 Raw 地址） | — |
| `dist/data/blue-fish-classification.json` | **仓库外导入流程** | 否 | legacy 形状，使用前归一到契约（原 `app.js` 的 `mapLocalRecord()` 已随单页应用退役） |

不进 git 的其余同门：`dist/data/`（图片与清单）、`staging/`、`.zcode/`、`小红书素材/`、已废弃的 `tools/deploy.mjs`。**例外是 `dist/owner-picks/`**——不在 `data/` 下，进 git，因为那批图的原图本来就在本仓库。

### 2.2 写路径只有两条

1. **GitHub Issue 投稿落库**：投稿者提 Issue → 维护者（或未来的自动审核 bot）按 [`数据契约.md`](数据契约.md) §10 校验后落库。投稿者全程不接触 git。
2. **维护者 git 提交**：改清单、加角色、加分类、调派生图，走 [`AGENTS.md`](AGENTS.md) 的 git 更新规范。

除此之外的写法（直接改服务器文件、用 `qss fs upload` 传数据、在前端里写死数据）都不是合法通道。

### 2.3 id 与命名约定

- **作品 id 不用文件名当 id**，按来源分前缀：`sticker_issue_01_001`（投稿）、`sticker_bf_001`（首批导入）、`sticker_op_001`（站长自用）、`sticker_001`（手写演示）。id 一经发布即冻结——它是评论串的钥匙；URL 走 `slug`（[`数据契约.md`](数据契约.md) §2.1 的冻结映射），**id 与 slug 解耦**。
- **投稿原图文件名** `issue-NN-NNN.<ext>`，同一次多图投稿按原顺序编号。
- **Giscus term = `sticker-<id>`**，term 与 id 绑定、**不随 URL 变**（见 §3）。
- **字段语义以 [`数据契约.md`](数据契约.md) 为准**，本文不定义任何字段。

### 2.4 分类边界

作品记录用 `categoryIds` 引用 `dist/categories.json` 的分类 id，跟 `characterId` 引用角色同一模式。**分类体系重建 = 只改 `categories.json` 与数据契约的「分类记录」章节**，不动记录结构、不动前端代码——这正是把分类迁出代码的目的。本轮只落数据边界：分类 UI 与既有记录的归类随「分类重建」一起做。

### 2.5 已知债务（动到相关代码前先知道）

- **（已清偿 2026-09-23）** 原 `app.js` 里 12 条手写演示数据（`sticker_001`–`sticker_012`）+ 168 条占位生成，随旧单页应用退役删除，不再有演示兜底。
- **`tone` / `symbol` 暂在记录层**：`tone` 的取值对齐 `--art-<tone>-*` Token 组（卡片容器底色），`symbol` 是图片加载失败时回退块里的字符。
- **`dist/data/blue-fish-classification.json` 会被外部流程重新生成**：任何对它的加工必须做成幂等、可重跑的脚本，否则下次导入就被覆盖。`localPreviewPath()` 从清单的 `previewPath` 取文件名拼本地路径，换预览图扩展名必须同步改清单。

## 3. 代码边界（前端）

前端是**多页静态站**（2026-09-23 多页改版）：手写页（`index.html`、分类 / 投稿 / 关于 / 推荐 / 更新日志）+ 生成页（`works/<slug>.html`，由 `tools/generate_work_pages.mjs` 从清单渲染）。旧单页应用 `dist/app.js` **已退役删除**，hash 路由只剩 `index.html` 里的内联转跳脚本——把 `#/work/<id>`、`#/character/<id>` 转到新页面，是老链接的兼容层，不许拿它长出新路由。改这一层先守这几条：

- **详情页只由生成器产出，不手改 `works/`。** 字段渲染、长尾文案公式、SEO 头集中在 `tools/generate_work_pages.mjs` 一处（口径 [`docs/SEO规范.md`](docs/SEO规范.md)）；手改生成物会被下次重跑全量重建冲掉。
- **数据加载收敛在一处**（清单按 `cache: "no-cache"` 拉取），视图只消费加载结果，不自己另起请求口径。
- **用户内容插值必须走 `escapeHtml`。** 作品名、描述、Tag、投稿者、来源链接都是外部输入，进 HTML 前（含生成期）一律转义；图标用内联 SVG，不用 emoji。
- **Giscus 是硬规则**：`data-mapping="specific"`、`data-term="sticker-<id>"`——**term 与 id 绑定，不随 URL 变**（URL 走 slug）。原警告保留：一旦 term 改用 slug、或退回默认 `pathname` 映射，全站评论会被挤进同一个讨论串、已有讨论串跟作品对不上。改路由 / 改 URL 方案时必须回来保住「一张作品一个串」的对应（[`数据契约.md`](数据契约.md) §12）。
- 评论接入点与投稿入口固定在生成器 / 页面模板一处；换评论服务、改模板文件名只改那里。

## 4. 样式边界（为风格大改立的基准）

三层，值只在最上层出现一次：

1. **`tokens.css` 是唯一定值处**——颜色、字号、间距、圆角、阴影、动效时长全在这里定义；
2. **`styles.css` 只引用语义变量**（`--text-primary`、`--bg-surface`、`--space-4`…）；
3. **组件 class** 消费语义变量，不自己发明值。

配套规则：小屏差异**优先重定义 Token**（`tokens.css` 末尾已有 1080 / 760 两档收缩），其次才写断点；图标内联 SVG，不用 emoji；主题靠 `data-theme="light|dark"` 切换，深浅两套语义层都在 `tokens.css`，组件层不感知主题。

**风格 / 布局大改的许可范围**：

| 可以 | 不可破 |
|---|---|
| 重排组件结构、改 DOM 骨架 | `styles.css` 里不出现硬编码色值 |
| 重写 `styles.css` 的组件层 | 间距 / 字号 / 圆角 / 阴影 / 动效时长一律 Token |
| 改 Token 的值、增删 Token | 改前端三件套必须同步 bump `index.html` 的 `?v=` |
| 重排 `tokens.css` 的分组与命名（同步改引用处） | 图标仍是内联 SVG，不用 emoji 图标当装饰 |

## 5. 构建与发布边界

- **`tools/build.mjs` 的契约**：零依赖（Node 内置模块 only）、确定性复制（纯字节拷贝，同一输入两次产物 SHA-256 一致）、跳过 `dist/data/` 与 `dist/submissions/originals/`。复制后跑断言：必需文件齐全、被排除目录确实不存在、两份 `works.json` 可解析、展示记录引用的图真实存在，**现含角色 / 分类清单形状合法且 id 唯一、记录的 `characterId` / `categoryIds` 可解析、投稿模板下拉与清单一致、清单 `slug` 与 `works/<slug>.html` 一一对应（防断链）**——任一不过就删产物、非零退出，坏数据发不出去。
- **发布拓扑**见 [`docs/维护与发布.md`](docs/维护与发布.md)：GitHub 驱动（本地推送 `main` → 服务器工作树拉取 → 构建 → `releases/<时间戳>` 原子切 `current`），失败自动把 `current` 指回旧目标；`shared/data` 硬链接进每个 release，是首批数据的唯一持久副本；**`cp -al` 之后不许再 chmod 任何产物**（硬链接共享 inode 与权限）。回滚只切软链，不重建、不删文件。
- **缓存分层与 `?v=` 是硬规则**：带 `?v=` 的 js / css 响应是 `public, max-age=31536000, immutable`——**`?v=` 就是指纹**，改了三件套不 bump，访问者最长一年拿旧缓存。清单与 HTML 永远回源校验，图片 7 天。
- **文档不进产物**（产物 = `dist/` 的构建输出）：只改文档不需要发布。发布是维护者在服务器上跑 `ops/deploy-server.sh` 的副本，**推送 ≠ 发布**。

## 6. 外部服务边界

| 服务 | 承担什么 | 边界 |
|---|---|---|
| GitHub | 投稿写入口（Issue Forms）、原图托管（`raw.githubusercontent.com`）、源码与清单仓库 | `rawGithubPath(repo, path)` 的**仓库参数按记录传**：首批原图在上游 `EDMOK/blue-fish-archive`，投稿与站长自用的原图在本仓库；写死一个仓库名会让下载指向错处 |
| Giscus | 评论（GitHub Discussions） | 复用 [`lmy414/lmy414-blog-comments`](https://github.com/lmy414/lmy414-blog-comments) 的 `Announcements` 分类，不为本站另开评论仓库；评论数据不回流本站（[`数据契约.md`](数据契约.md) §12） |
| Google Analytics 4 | 聚合的访问来源、设备与页面性能分析 | Google tag `GT-NS92WVPQ`，GA4 数据流 `G-4LWN9Z2WT2`；广告个性化 / Google Signals / 广告存储关闭；统计结果不写进作品清单、不生成热度分或排行榜 |
| Google Fonts | 手写字体文件加载 | 仅提供界面字体，外部请求与 GA4 / Cookie 口径在 README 与关于页公开 |
| 未来的判断服务（Jev / 视觉模型 / LLM） | 投稿自动审核的判断与回复 | **只允许存在于服务器侧自动审核 bot 层**：不进前端代码、不进清单、不进发布产物。影子模式先行，见 §10 |

**外部服务的准入门槛是最小化统计与透明披露**（[`数据契约.md`](数据契约.md) §11）：GA4 只用于聚合访问来源、设备与性能优化，广告个性化 / Google Signals / 广告存储关闭，不把统计结果写入作品数据。带广告、用户画像或不透明追踪的服务仍不接入。

## 7. 硬性规则总表

| 规则 | 为什么 |
|---|---|
| 改 `styles.css` / `tokens.css` 必须同步 bump 全部 HTML 引用处的 `?v=` | 带 `?v=` 的 js / css 是一年 immutable，不 bump 等于让访问者拿旧缓存，你会以为修复没生效 |
| 样式只消费 Token，`styles.css` 不出现硬编码色值 | 值只在 `tokens.css` 一处，风格大改和深浅主题不用全局搜改 |
| Giscus `data-mapping=specific`、`data-term=sticker-<id>` | **term 与 id 绑定，不随 URL 变**（URL 走 slug）；换映射或改 term 会把全站评论挤进同一个讨论串，已有讨论串也会跟作品对不上 |
| `rawGithubPath` 的仓库参数按记录传 | 首批原图在上游 `EDMOK/blue-fish-archive`、投稿原图在本仓库，写死会让原图链接指向错的仓库 |
| `owner-picks` 不进投稿角色下拉、不配品牌色 Token | 它借 `characters` 结构做分类，但不是角色；用的是 `--art-owner-picks-*` 那组中性石板蓝 |
| 显式列文件名暂存，禁止 `git add -A` / `git add .` | 工作区里躺着 `dist/data/`、`staging/`、`.zcode/`、`小红书素材/`、`tools/deploy.mjs` 这些不入库内容，一把梭会扫进仓库 |
| 中文域名在脚本、日志和 `og:url` 里一律用 punycode `xn--pssy23gqgbz2d718b.com` | 同一个站两种写法在不同工具里编码不一致，以 punycode 为准 |
| 不删任何 release、`shared/data`、`acme` | 回滚靠旧 release；`shared/data` 是首批数据唯一持久副本（硬链接共享 inode）；`acme/` 是证书验证目录 |
| `cp -al` 之后不许 chmod 任何产物 | 硬链接共享 inode 与权限，会把 shared 数据和所有旧 release 的权限一起改坏 |
| 大更新先在 [`CHANGELOG.md`](CHANGELOG.md) 补一条，再动代码或服务器配置 | 那条记录是动手前的自检：写不出「为什么改」，通常说明还没想清楚 |
| 投稿原图不进发布产物 | 原件走 GitHub Raw 下载，产物保持小（发布包 73.7 MB → 8.3 MB 的来源） |

## 8. 变更矩阵

| 想改 X | 动哪些文件 / 步骤 |
|---|---|
| 新增角色 | `dist/characters.json` 加一条 → `node tools/sync_issue_template.mjs --write` 同步投稿下拉 →（可选）`tokens.css` 补 `--art-<id>-*` 色板（动了 Token 就要 bump `?v=`），不补沿用 `other` |
| 新增分类 | `dist/categories.json` 加一条（记录侧用 `categoryIds` 引用，UI 随分类重建） |
| 新增作品记录字段 | [`数据契约.md`](数据契约.md) 补字段定义 → 各生成方（投稿落库 / 导入流程 / 派生脚本）写入 → `tools/build.mjs` 补引用断言 |
| 加作品详情页字段 | [`数据契约.md`](数据契约.md) 补定义 → 改生成器 `tools/generate_work_pages.mjs`（渲染与 SEO 头都在这一处）→ 重跑生成器 → `tools/build.mjs` 断言过 |
| 改风格（配色 / 字号 / 间距…） | `tokens.css`（必要时 `styles.css`）→ bump `?v=` → 本地起服务浏览器验收 |
| 改布局 / 视图 | 手写页改对应 HTML；详情页改 `tools/generate_work_pages.mjs` 后重跑 → 样式改动走 `styles.css` → bump `?v=` → 浏览器验收（[`CONTRIBUTING.md`](CONTRIBUTING.md) §二 清单） |
| 改发布方式 | `ops/` 脚本 + [`docs/维护与发布.md`](docs/维护与发布.md) + [`CHANGELOG.md`](CHANGELOG.md) 先记一条 |
| 换评论服务 | 评论接入点（生成器 / 页面模板一处）加分支，**保留 `sticker-<id>` 的串对应** |
| 接入投稿自动化 | `ops/` 下新增服务器侧 bot 层（**不进前端、不进清单**），影子模式先行 |

## 9. 已定方向（不用再问）

- **数据用 JSON 清单，不上数据库。** 引入后端的触发条件：记录量级到数千、多管理员并发写入、站内实时投稿、账号体系。届时先上轻量数据库 + 索引 API（[`站点升级路线图.md`](站点升级路线图.md) 1C），原图字节仍不经后端。
- **暂不换前端框架，原生 ES 模块拆分先行。** 换框架的触发条件：内容页增多且 SEO / 分享预览成为瓶颈、出现第二位前端贡献者、交互复杂度爆炸。届时首选 Astro。
- **写入口固定 GitHub Issue**，不开放站内匿名上传。
- **发布批量进行，推送 ≠ 发布**；发布是维护者在服务器跑 `ops/deploy-server.sh`。
- **统计只做网站优化，不做作品热度。** GA4 可记录聚合的页面访问、来源、设备与性能数据；不把访问量 / 点赞 / 收藏 / 下载写回作品记录，不做热度分或排行榜（[`数据契约.md`](数据契约.md) §11）。
- **图片只放 GitHub 仓库**，不自建对象存储或 CDN；仓库真装不下了才讨论自托管。

## 10. 待决事项队列

| 事项 | 现状 |
|---|---|
| 分类枚举定稿 | **已落地（2026-09-23）**：`meme` 梗图 / `illustration` 插画 / `setting` 设定图；记录通过 `categoryIds` 引用 |
| 风格 / 布局大改的方向输入 | 边界已立（§4），等方向输入开工 |
| 每作品静态页 + OG 分享卡 | **已落地（2026-09-23）**：每作品 `works/<slug>` 独立静态页 + OG / Twitter 分享卡，口径 [`docs/SEO规范.md`](docs/SEO规范.md)，本行留档 |
| `app.js` 模块拆分 | **已作废（2026-09-23）**：`app.js` 随旧单页应用退役删除，多页 + 生成器取代了原拆分目标 |
| 投稿自动审核 | 视觉模型看图 → Jev 判断 → LLM 回复，影子模式先行；只进服务器侧 bot 层（§6） |
