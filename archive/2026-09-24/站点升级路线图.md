# 蓝色大肥鱼 · 站点升级路线图

> 文档状态：规划基线
>
> 建立日期：2026-09-21
>
> 当前版本：第一期平台化升级规划

这份文档记录站点从当前的「无构建纯静态前端」逐步升级为「可构建静态前端 + 索引 API + 数据库」的路线、边界和验收标准。（2026-09-22 起已有零依赖构建入口 `tools/build.mjs`，但前端源码仍在 `dist/`，未做 `src/` 迁移。）

**本文件只记录规划，不代表下列功能已经上线。** 当前线上站点仍以 `dist/` 为根目录，作品数据仍来自现有清单，图片仍按当前发布流程提供。

---

## 1. 先说结论

### 1.1 你的总体方向可行，但顺序要调整

引入构建流程、后端 API 和索引数据库，能够解决以下长期问题：

- 前端所有数据和视图挤在一个 `app.js` 中，修改成本越来越高；
- 前端正常打开时全量加载清单，后续记录增长后会越来越慢；
- 投稿、审核、图片处理和未来管理能力没有稳定的服务端边界；
- 以后要接入性能监控、错误监控或其他服务能力时，没有统一的接口和部署方式。

但数据库本身不是当前首屏速度的第一杠杆。2026-09-21 的基线显示：

- 三份清单合计约 187 KB；
- 默认首页前 24 张投稿卡片约请求 46.5 MB；
- 投稿的 `thumbnailPath` 和 `fullPath` 目前都指向原图；
- 因此必须先修复图片资产链路，再把数据库分页接进来。

### 1.2 推荐的图片链路

```text
首页列表
  浏览器 → 480px 左右的缩略图

作品详情
  浏览器 → 约 1280px 的详情显示图

查看原图 / 下载原图
  浏览器 → GitHub Raw 原图

作品数据、搜索、筛选、审核状态
  浏览器 → API → 索引数据库
```

后端**不代理原图字节**。API 只返回经过白名单处理的图片 URL 或图片定位信息，浏览器直接访问 GitHub 原图。这样可以避免：

- 后端多一次下载和转发；
- 自有服务器承担原图带宽；
- GitHub、后端和浏览器之间形成难以排查的超时链路；
- 把图片服务和数据库服务绑成一个单点故障。

GitHub 仍然保存原图。缩略图和详情图属于可重建的派生资产，第一期沿用现有静态资源发布方式；它们不应成为数据库里的二进制，也不应让数据库成为唯一图片来源。

### 1.3 「取消无构建」的准确含义

目标不是把所有页面改成后端渲染，而是：

- 前端源码进入可复现构建流程；
- 构建产物仍是静态文件，由 nginx 或其他静态托管提供；
- JavaScript、CSS 和派生图片使用内容指纹并长期缓存；
- API 只承担动态数据和服务能力；
- 前端在 API 故障时有明确的降级或静态快照方案。

第一版不强制把现有原生 DOM 交互改写成 React 或 Vue。先做模块化和构建化，再根据实际复杂度决定是否更换 UI 框架。

---

## 2. 当前基线

| 项目 | 当前事实 | 证据 / 影响 |
|---|---|---|
| 前端形态 | `dist/` 即站点根目录，无前端框架、无后端；2026-09-22 起有零依赖构建入口 `tools/build.mjs`，前端源码仍在 `dist/` | `AGENTS.md`、`README.md` |
| 前端代码 | 数据、路由、视图、评论集中在 `dist/app.js` | 后续拆分是 1B 的主要工作 |
| 展示记录 | 实际约 185 条：首批 146、投稿 35、站长自用 4 | 清单过滤后进入公开展示的数量 |
| 首页分页 | 客户端持有全部 `published` 记录，首批显示 24 条 | `数据契约.md` §9 |
| 首屏图片 | 最新排序使首页前 24 张主要落到投稿原图，约 46.5 MB | 当前最大性能瓶颈 |
| 投稿图片 | 35 张原图约 63.7 MB，缩略图字段实际指向原图 | 违反 `数据契约.md` §3 的字段语义 |
| 清单请求 | 三份 JSON 一次性加载，`fetchJson()` 使用 `cache: "no-store"` | 每次进入都会重新拉取 |
| 首批数据 | 清单由仓库外导入流程生成，`dist/data/` 不进 git | 迁移和加工必须可重跑 |
| ID 风险 | 首批 `sticker_bf_<序号>` 由数组顺序生成 | 迁移前必须冻结 ID，否则会破坏旧链接和评论串 |
| 原图来源 | 首批依赖 `EDMOK/blue-fish-archive`，投稿和站长自用使用本仓库 Raw | 仓库参数必须按记录保存，不能写死 |
| 发布方式 | GitHub 驱动：本地推送 `main` → 服务器工作树拉取 → `tools/build.mjs` 构建干净 release → 原子切 `current`；发布/回滚脚本在 `ops/`。首版已上线 `releases/20260922-145230`（commit `a684d56`） | 后端上线后需增加迁移、重启和健康检查 |
| 统计承诺 | 当前契约明确不做热度统计和追踪 | 任何统计功能都要单独升级契约和公开文案 |

性能基线、已知图片问题和当前缓存现状详见 [`CHANGELOG.md`](CHANGELOG.md) 的「加载优化立项」以及 [`AGENTS.md`](AGENTS.md) 的「加载现状与已知问题」。

---

## 3. 阶段进度表

状态说明：

- **已完成**：已具备可核对的实现或文档结果；
- **规划完成**：方案已确定，但尚未开始实施；
- **进行中**：该阶段已有实际改动；
- **未开始**：尚未进入实施；
- **阻塞**：需要先解决明确的前置决策或环境问题。

| 阶段 | 目标 | 主要交付物 | 当前状态 | 通过标准 |
|---|---|---|---|---|
| 0. 基线与契约 | 冻结事实、ID 和数据边界 | 本路线图、性能基线、稳定 ID 映射方案、脱敏规则、回滚方案 | **规划完成** | 任何后续改动都能回答“数据从哪来、图片从哪来、如何回滚” |
| 1A. 资产与传输提速 | 先解决首屏几十 MB 图片请求 | 480px 缩略图、约 1280px 详情图、动图派生图、前端加载属性 | **已完成**：已上线 `releases/20260922-023440`；服务器压缩、HTTP/2 与缓存分层于 2026-09-22 收尾（改 nginx vhost，不产生新 release） | 线上首页不请求投稿原图（实测 0 次），首屏投稿缩略图约 0.42 MB；发布包 73.7 MB → 8.3 MB。nginx 侧已收尾：gzip 覆盖 css / js / json 等文本类型、HTTP/2 实测可用、缓存分层（`?v=` 资源一年 immutable / 清单与 HTML 回源校验 / 图片 7 天）；Brotli 因服务器无 ngx_brotli 模块未启用 |
| 1B. 前端构建化 | 结束单文件、无构建形态 | Vite + TypeScript（或等价方案）、模块拆分、指纹化产物 | **进行中**：第一版零依赖构建（`tools/build.mjs`）与 GitHub 驱动发布链路已落地，首版已于 2026-09-22 上线 `releases/20260922-145230`；Vite + TypeScript 模块化仍未开始 | 可一键构建；现有路由、主题、瀑布流、评论和下载行为不回归 |
| 1C. 后端索引 API | 不再从前端全量加载 JSON | 数据库迁移、API v1、游标分页、搜索、筛选、详情接口 | **未开始** | 首页只请求一页；旧清单与 API 的数量、筛选、排序和详情结果一致 |
| 1D. 双轨切换 | 低风险上线新数据源 | 新旧数据并行校验、功能开关、健康检查、静态快照兜底 | **未开始** | API 异常可降级；确认稳定后才移除旧主链路 |
| 2. 视觉与内容体验 | 缓解页面单调并提高信息层级 | 首页信息架构、分类入口、骨架屏、空状态、详情页和移动端优化 | **未开始** | 亮色/暗色、桌面/移动、键盘操作和视觉验收均通过 |
| 3. 可观测性与统计评估 | 能发现性能和错误，不默认制造热度排行 | Web Vitals、API 延迟/错误率、匿名聚合方案评估 | **未开始** | 先明确隐私、保留周期、Cookie 和用户标识策略，再决定是否上线 |
| 4. 投稿渠道升级 | 降低人工收录成本 | Issue 自动校验、派生图生成、审核队列；后续再评估站内投稿 | **未开始** | 格式、大小、解码、查重、角色、标签、授权、限流和状态均可追踪 |
| 5. 管理与高级扩展 | 支撑长期维护 | 管理后台、批量修订、下架审计、SEO/预渲染等 | **未开始** | 权限、审计、备份恢复和回滚成熟后再开放 |

### 推荐实施顺序

```text
0 基线与契约
  ↓
1A 图片资产与传输优化 ─────┐
  ↓                         │
1B 前端构建化                ├─ 1D 双轨上线
  ↓                         │
1C 后端索引 API ────────────┘
  ↓
2 视觉体验 → 3 可观测性 → 4 投稿自动化 → 5 管理能力
```

1A、1B、1C 属于同一个“大版本方向”，但必须保留独立发布和回滚能力。不要为了等待后端而延迟图片修复，也不要在数据库尚未核对时直接删除旧清单。

---

## 4. 第一期实施拆解

### 4.1 1A：资产与传输提速

#### 工作项

- 建立幂等、可重跑的图片处理脚本；
- 静态图生成约 480px 的列表缩略图和不超过约 1280px 的详情图；
- 动图生成可控尺寸、帧数和体积的动画 WebP 或其他明确支持的派生格式；
- 修正 `dist/submissions/works.json` 的 `thumbnailPath` 和 `fullPath`；
- 同步处理首批 `previews/` 目录中体积异常的 4 张 GIF；
- 保留原图下载路径，列表和详情不再默认请求原图；
- 拆分轻量头像资源，避免每张卡片重复使用 262 KB 的 `favicon.png`；
- 为图片补充 `decoding`、合理的 `fetchpriority` 和必要的 `srcset` / `sizes`；
- 开启 gzip 或 Brotli（以线上环境实际支持为准）和 HTTP/2；
- 为 HTML、清单、指纹化脚本、样式和图片设置分层缓存；
- 用浏览器网络瀑布和移动网络模拟验证，而不是只检查文件是否生成。

#### 约束

- 不把原图交给后端代理；
- 不把派生图做成数据库二进制字段；
- 生成脚本必须重复执行不产生重复资产或路径漂移；
- 涉及 `dist/submissions/` 的修改属于公开数据变更；
- 涉及 `dist/data/` 的修改必须能承受上游清单重新生成；
- 修改 `app.js`、样式或 Token 时，必须同步更新 `index.html` 中的 `?v=`。

#### 验收门槛

- 首页前 24 张卡片的网络请求中不存在投稿原图；
- 首屏图片总传输量目标从约 46.5 MB 降到 1.5–2 MB 以内，若动图内容导致无法达标，必须记录例外；
- 详情页默认请求显示图而不是原图；
- 明确点击下载时才请求 GitHub 原图；
- 低速移动网络下 LCP 目标暂定低于 2.5 秒，若基线设备无法达到，记录设备、网络和瓶颈；
- 缩略图加载失败时有可读的回退状态，不能出现破图占位；
- 现有 Giscus 评论、下载、hash 路由和深浅主题不回归。

### 4.2 1B：前端构建化

#### 推荐结构

```text
apps/
  web/                 前端源码和构建入口
  api/                 后端服务（1C 再启用）
packages/
  contracts/           API DTO、枚举、校验规则
  image-pipeline/      图片校验与派生图生成
  config/              可公开共享配置
migrations/            数据库迁移
.env.example           变量名和占位值，不放真实秘密
```

第一版优先把现有代码拆成配置、数据访问、路由、图片、视图、评论和主题模块，不同时进行大规模 UI 重写。现有 Giscus 的 `specific` 映射和 `sticker-<id>` term 必须保留。

#### 验收门槛

- 新环境可通过文档中的单条命令安装并构建；
- 构建结果可部署到现有静态根目录或等价目录；
- JS/CSS 使用内容指纹，可安全长期缓存；
- 旧版 release 仍可独立回滚；
- 生产包不包含 `.env`、数据库、Issue 附件、管理备注或内部部署信息；
- 现有页面路由、搜索、筛选、排序、瀑布流、评论、下载和主题通过回归检查。

### 4.3 1C：后端索引 API

#### 数据职责

| 数据层 | 负责什么 | 是否作为图片唯一来源 |
|---|---|---:|
| GitHub 仓库 | 原图、公开源码、必要的可审计资产 | 原图来源是 |
| 数据库 | 公开元数据、状态、标签、搜索和排序索引 | 否 |
| 静态派生资源 | 缩略图、详情图等可重建文件 | 否 |
| 公开快照 | 脱敏导出、备份和故障降级 | 否 |
| 审计与临时数据 | 审核备注、原始附件、管理日志 | 不公开 |

#### 数据库建议

当前公开数据规模约 185 条，第一版可采用 SQLite + WAL + 版本化迁移；如果后续出现多实例写入、较高并发或托管要求，再评估 PostgreSQL。数据库文件、备份和日志不进公开仓库。

建议核心表：

- `stickers`：稳定 ID、名称、描述、角色、状态、来源、授权和时间；
- `sticker_assets`：缩略图、详情图、原图的仓库、引用版本、路径、格式、尺寸、体积和哈希；
- `characters`：角色名称、别名和状态；
- `tags`、`sticker_tags`：标准化标签关系；
- `submissions`：待审核投稿，与已发布作品分离；
- `audit_logs`：管理操作审计，不进入公开导出。

不要继续把所有图片定位塞进一个 `path`：

- `original_repo`、`original_ref`、`original_path`：原图的 GitHub 定位；
- `thumbnail_path`：列表使用的派生图；
- `display_path`：详情使用的派生图；
- 各资产分别记录格式、宽高、体积和校验值。

原图定位优先保存提交 SHA 或其他不可变引用，不要只保存永远变化的 `main`。首批现有 ID 不能重新编号：必须先建立“源路径或 SHA-256 → 已分配 ID”的冻结映射。

#### 第一批只读 API

```text
GET /api/v1/stickers
  ?cursor=&limit=24&q=&character=&format=&animated=&sort=

GET /api/v1/stickers/:id
GET /api/v1/characters
GET /api/v1/meta
```

返回给公开前端的 DTO 只包含必要字段，例如：

```json
{
  "id": "sticker_issue_01_001",
  "name": "作品名称",
  "description": "可选描述",
  "characterId": "deepseek",
  "tags": ["吐槽"],
  "isAnimated": false,
  "thumbnailUrl": "…",
  "displayUrl": "…",
  "originalUrl": "…",
  "width": 1024,
  "height": 1024,
  "status": "published"
}
```

API 不直接序列化数据库整行，不返回投稿审核备注、内部路径、访问日志、管理账号、令牌或其他内部字段。

#### API 验收门槛

- 首页默认只获取一页数据，而不是三份全量 JSON；
- 搜索覆盖契约要求的名称、描述、Tag、角色名、角色别名和投稿者；
- 角色、格式、静态/动态筛选和现有排序结果与旧前端一致；
- `published` 以外的状态不会出现在公开接口；
- 旧作品 ID、详情链接和 Giscus term 不变化；
- API 失败时前端显示可理解的降级状态，并可选择读取脱敏静态快照；
- 分页使用游标或稳定排序键，避免新增作品导致 offset 分页重复或漏项。

### 4.4 1D：双轨切换

上线顺序：

1. 将现有清单导入数据库，不改变公开前端；
2. 对比作品总数、角色计数、搜索、筛选、排序和详情字段；
3. 记录差异并修正导入或映射，不用“看起来差不多”作为通过标准；
4. 以功能开关让少量请求读取 API；
5. 观察 API 错误、响应时间、图片请求和数据差异；
6. 保留静态快照和旧 release，确认稳定后再扩大比例；
7. 最后才考虑移除旧的正常路径。

后端发布步骤必须比当前静态发布多出：数据库备份、兼容迁移、服务启动或重启、健康检查、API 回读和回滚说明。所有服务器写操作继续遵守 qss 确认门和审计要求。

---

## 5. 后端开源与脱敏边界

### 可以公开

- 前后端源码；
- 数据库 schema、迁移和 API 契约；
- 图片校验与派生图生成脚本；
- `.env.example`（只有变量名和占位值）；
- 已发布作品的脱敏公共导出；
- 不含生产密钥的本地开发说明。

### 不得进入仓库

- `.env`、数据库文件、备份和恢复包；
- GitHub App 私钥、PAT、管理 Token、会话密钥；
- 临时上传文件、待审核原始附件；
- 原始访问日志、完整 IP、User-Agent、请求正文和 Cookie；
- 管理账号、审核备注、风控结果和内部路径；
- 生产服务器地址、面板凭据和内部部署配置；
- 未脱敏的 Issue 导出或个人联系信息。

### 最低安全要求

- API 使用显式公开 DTO，不直接暴露数据库记录；
- 投稿或自动化使用最小权限 GitHub App，避免使用个人 PAT；
- 请求体上限、速率限制、同源 CORS 和错误响应脱敏；
- 图片格式白名单、文件大小限制、魔数检测、正常解码检查和 SHA-256 查重；
- 对压缩炸弹、异常扩展名、可执行文件和伪造 MIME 类型做拒绝；
- 日志不记录令牌、Cookie、完整 IP 和上传正文；
- CI 增加 secret scanning、依赖审计和迁移检查；
- 数据库迁移采用向前兼容的 expand/contract 方式，保证旧 release 可回滚。

---

## 6. 统计与投稿的单独决策点

### 统计

当前 [`数据契约.md`](数据契约.md) §11 明确不统计 PV/UV、点赞、收藏、分享、下载次数和热度分，也承诺零追踪。接入统计不是简单增加一段脚本，而是产品政策变更。

推荐顺序：

1. 先做不带用户画像的 Web Vitals、API 延迟、错误率和资源失败率；
2. 再单独评估匿名聚合访问统计；
3. 是否记录单图浏览或下载必须另立决策；
4. 不把任何新指标自动用于热度排序、排行榜或作品曝光竞争。

任何统计上线前必须同步修改数据契约、隐私说明、README、关于页和更新日志，并明确保留周期、Cookie、用户标识和删除方式。

### 投稿

第一步优先自动化现有 GitHub Issue 流程，而不是立即开放匿名上传：

```text
读取待处理 Issue
  → 校验附件和字段
  → SHA-256 查重
  → 生成缩略图与详情图
  → 输出审核清单
  → 维护者确认
  → 发布到 GitHub / 静态资源 / 数据库
```

站内投稿、临时上传、Turnstile、审核后台和 GitHub App 写入，放在自动化流程稳定之后再做。这样可以先降低人工重复劳动，不立即引入匿名文件上传的安全面。

---

## 7. 主要风险、停止条件和回滚

| 风险 | 表现 | 防护 | 停止条件 |
|---|---|---|---|
| 只上数据库不做缩略图 | 首屏仍请求几十 MB | 1A 必须先过图片网络验收 | 首页仍出现投稿原图请求 |
| 后端代理原图 | 带宽和延迟增加 | API 返回 URL，浏览器直连 GitHub | 发现 API 响应原图字节 |
| ID 被重新编号 | 旧链接和评论串错位 | 迁移前冻结映射并做差异检查 | 任一既有 ID 改变 |
| API 成为单点故障 | 页面无法展示 | 静态快照、健康检查和降级状态 | 无法从旧 release 回滚 |
| GitHub 原图失效 | 下载功能失效 | 保存 repo/ref/path，记录外部依赖 | 不清楚原图真实来源 |
| 派生图不可重建 | 上游重导入后路径断裂 | 幂等脚本、内容哈希、生成记录 | 第二次运行产生不同结果 |
| 脱敏失败 | 密钥或个人信息进入公开仓库 | `.env.example`、secret scan、发布前检查 | 发现真实凭据或审核数据 |
| 统计越过产品边界 | 与零追踪承诺冲突 | 先改契约和公开文案 | 未完成隐私评估就上线 |
| 后端发布不可回滚 | 新旧版本无法共存 | 备份、迁移兼容、健康检查 | 旧 release 不能独立运行 |

### 回滚原则

- 1A：回滚静态资源 release，不删除旧派生资产；
- 1B：保留旧静态构建产物，切回上一版 `current` 用 `ops/rollback-server.sh <release目录名>`（只切软链，不重建不删文件）；
- 1C：迁移必须可逆或向前兼容，数据库恢复前先保留备份；
- 1D：先关闭 API 功能开关，再切回旧前端和旧清单；
- 服务器回滚只改软链或服务版本，不删除任何 release。

---

## 8. 每阶段通用验收模板

每个阶段合并前补齐以下记录：

- [ ] 改动范围和不改范围；
- [ ] 数据契约是否变化；
- [ ] 是否新增公开字段、API 或环境变量；
- [ ] 是否产生需要脱敏的文件；
- [ ] 是否保留旧版本回滚路径；
- [ ] 关键性能基线与改后数据；
- [ ] 桌面、移动和低速网络检查；
- [ ] 现有 hash 路由与 `sticker-<id>` 评论映射检查；
- [ ] 图像失败、API 失败和清单失败时的降级检查；
- [ ] `CHANGELOG.md` 是否已先记录大型变更；
- [ ] 如改动 `app.js` / `styles.css` / `tokens.css`，`index.html` 的 `?v=` 是否同步；
- [ ] 公开仓库检查：无 `.env`、数据库、密钥、Issue 附件、内部日志和生产配置；
- [ ] 本地服务启动并在浏览器中查看过，而不是只读代码判断。

---

## 9. 下一步任务顺序

本文件完成后，下一次实施应按下面的顺序开始，而不是直接创建后端：

1. 固定首批作品的稳定 ID 映射和源路径映射；
2. 记录首页与详情页的浏览器网络基线；
3. 实现并在本地重复运行图片派生处理脚本；
4. 先修正投稿缩略图和首批异常 GIF；
5. 在浏览器里确认首页不请求投稿原图；
6. 再决定构建工具和前端目录迁移方式；
7. 最后按旧清单导入数据库并做双轨差异校验。

任何涉及代码、图片、JSON、服务器或部署方式的下一阶段，都要单独在 `CHANGELOG.md` 记录后再实施。