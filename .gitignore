# bluedafeiyu —— 站点源码仓库
#
# 这个仓库跟踪「站点源码 + 结构化数据」：dist/ 下的页面 / 样式 / 脚本、tools/、ops/、
# archive/，以及 data/ 下的清单。图片一律来自内容仓库 lmy414/ai-girl-stickers，不在这里跟踪。

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# 依赖与本地运行时产物
node_modules/
__pycache__/
.sites-runtime/

# 本地配置与密钥
.env
.env.*
!.env.example
*.log

# 构建产物
/.build/

# 可选：把内容仓库克隆到本仓库同级 content/ 时，整个目录不进 git
/content/

# 并行子代理的 git worktree（不进仓库）
/.worktrees/

# ---------------------------------------------------------------- 结构化数据（跟踪）
# data/ 是角色 / 分类 / 投稿清单 / id 冻结映射 / 首批编辑叠加层的权威副本，
# **进 git**。构建期由 tools/stage_data.mjs 暂存成 dist/ 下的产物路径。
# 这里只登记占位说明，不加忽略规则：

# ---------------------------------------------------------------- 内容仓库同步物
# tools/sync_content.mjs 从内容仓库同步进来的图片与投稿模板副本：一次都不跟踪。
# 注意 data/ 下的清单不再从内容仓库同步——它们在仓库根 data/ 里跟踪，
# 由 tools/stage_data.mjs 暂存成下面这些 dist/ 路径。
/dist/characters.json
/dist/categories.json
/dist/blue-fish-ids.json
/dist/favicon.ico
/dist/favicon.png
/dist/avatar.png
/dist/data/
/dist/submissions/
/dist/owner-picks/
# 投稿模板副本（内容仓库根的 .github/ 同步进来），构建期只读、不进发布包
/dist/.github/

# ---------------------------------------------------------------- 构建生成物
# tools/build_site_snapshot.mjs / generate_work_pages.mjs / generate_sitemap.mjs 的产出
/dist/site-data.json
/dist/site-data.js
/dist/sitemap.xml
/dist/works/

# ---------------------------------------------------------------- 图片
# 本仓库不跟踪任何图片位图；图片归内容仓库
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.ico
